# Plan

Accessibility (a11y)

At scale, styling decisions need a11y baked in (e.g., focus-visible rings,
reduced motion variants, high-contrast themes).

**Features**:

- Prebundled accessibility variants (like focusStyle, highContrast).
- A config that automatically applies focus-visible:ring unless explicitly
  disabled.
